# ?? ?? ??
server:
  port: 8080

# database ?? ??
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_backendProject?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: user_sije3757
    password: sije9863
  thymeleaf:
    cache: false

  # spring data jpa ??
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update
